"use strict";

const { Users, Credentials } = require("./credentials");
const { Producation } = require("./producation");

module.exports = {
  UsersInfo: Users,
  CredentialsInfo: Credentials,
  ProducationInfo: Producation,
};
